{
    "name": "net.bansemir.profile",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "ui5 serve -o index.html",
        "build": "ui5 build --all --dest ../dist/app",
        "lint": "ui5lint"
    },
    "devDependencies": {
        "@ui5/cli": "^4.0.0",
        "ui5-middleware-livereload": "^3.0.0"
    }
}
